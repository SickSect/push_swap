#include "push_swap.h"

void	lst_connect(t_node **head_a, t_node **head_b)
{
    int	counter;

    counter = ft_lstlen(head_b);
    while(counter)
    {
		if((*head_a)->data )
    }
	printer(head_a, head_b);
}